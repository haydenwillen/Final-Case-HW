# Path to the dataset CSV (defaults to cfb23.csv in the repo)
DATA_PATH=/app/cfb23.csv

# Flask port (defaults to 5000)
FLASK_RUN_PORT=5000

# Matplotlib cache dir (kept in tmp inside the container)
MPLCONFIGDIR=/tmp/matplotlib
